import 'dart:html';

import 'package:flutter/material.dart';
class ThridActivity extends StatefulWidget {
  ThridActivity({Key key}) : super(key: key);

  @override
  _ThridActivityState createState() => _ThridActivityState();
}

class _ThridActivityState extends State<ThridActivity> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: Text("Hello"),
    );
  }
}